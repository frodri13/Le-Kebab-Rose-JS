import Menu from "./Menu";
import Options from "./Options";

function Carte() {
  return (
    <section className="carte">
      <Menu />
      <Options />
    </section>
  );
}

export default Carte;
